{% include 'default/_form.html.twig' with {
    page_title: 'edit_a_gallery'|trans({'%id%': gallery.id|default('')}),
    form: form,
    submit_label: 'save_index'|trans,
    back_to_list_path: 'gallery_index',
} %}