{% include 'default/_form.html.twig' with {
    page_title: 'delete_a_gallery'|trans({'%id%': gallery.id|default('')}),
    form: form,
    submit_label: 'do_delete'|trans,
    back_to_list_path: 'gallery_index',
} %}
