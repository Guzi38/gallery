{% extends 'base.html.twig' %}

{% block title %}
    {{ 'User details'|trans({'%id%': user.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'user_details'|trans({'%id%': user.id|default('')}) }}</h1>
    {% if user is defined and user|length %}
        <dl class="dl-horizontal">
            <dt>{{ 'Id'|trans }}</dt>
            <dd>{{ user.id }}</dd>
            <dt>{{ 'Email'|trans }}</dt>
            <dd>{{  user.email }}</dd>
            <dt>{{ 'user.password'|trans }}</dt>
            <dd>{{  user.password }}</dd>
        </dl>
        <p>
            <a href="{{ url('user_index') }}" title="{{ 'action.back_to_photos'|trans }}" class="{{ 'btn btn-light' }}">
                {{ 'back_to_previous_page'|trans }}
            </a>
        </p>
    {% else %}
        <p>
            {{ 'message.record_not_found'|trans }}
        </p>
    {% endif %}
{% endblock %}