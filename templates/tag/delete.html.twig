{% include 'default/_form.html.twig' with {
    page_title: 'delete_a_tag'|trans({'%id%': tag.id|default('')}),
    form: form,
    submit_label: 'do_delete'|trans,
    back_to_list_path: 'tag_index',
} %}
